import { Component, OnInit } from '@angular/core';
import { ActivatedRoute } from '@angular/router';
import { TranslatePipe } from '@ngx-translate/core';
import { ApiService, Booking, Property } from '../../../core/services/api.service';
import { BookingFormComponent } from '../../../shared/components/booking-form/booking-form.component';
@Component({ selector: 'app-property-detail', imports: [TranslatePipe, BookingFormComponent], template: `<section class="mx-auto grid max-w-6xl gap-6 px-4 lg:grid-cols-[1fr_380px]">@if (property) { <article class="space-y-4"><img [src]="property.images[0] || 'https://images.unsplash.com/photo-1564013799919-ab600027ffc6?auto=format&fit=crop&w=1200&q=80'" class="h-72 w-full rounded-[2rem] object-cover md:h-[28rem]" alt="" /><h1 class="text-3xl font-black">{{ property.title }}</h1><p class="text-slate-500">{{ property.city }}, {{ property.country }} · {{ property.max_guests }} {{ 'SEARCH.GUESTS' | translate }}</p><p class="leading-7">{{ property.description }}</p></article><aside class="lg:sticky lg:top-24 lg:self-start"><app-booking-form (submitted)="book($event)" />@if (booking) { <div class="mt-4 rounded-3xl bg-emerald-50 p-4 text-emerald-900"><p>{{ booking.status }}</p><button class="mt-3 rounded-full bg-emerald-600 px-4 py-2 font-semibold text-white" (click)="pay()">{{ 'PROPERTY.PAY' | translate }}</button></div> }</aside> }</section>` })
export class PropertyDetailComponent implements OnInit { property?: Property; booking?: Booking; constructor(private api: ApiService, private route: ActivatedRoute) {} ngOnInit(): void { const id = this.route.snapshot.paramMap.get('id')!; this.api.getProperty(id).subscribe(r => this.property = r.data); } book(dates: { start_date: string; end_date: string }): void { if (!this.property) return; this.api.createBooking({ property_id: this.property.id, ...dates }).subscribe(r => this.booking = r.data); } pay(): void { const paymentId = this.booking?.payment?.id; if (paymentId) this.api.createPaymentIntent(paymentId).subscribe(); } }
